package ExerciciosClassEEncapsulamento.ContaBancaria;

/*class ContaBancaria {
    constructor(depositoInicial){
        this.saldo = depositoInicial
        this.chequeEspecial = depositoInicial <= 500 ? 50 : depositoInicial * 0.5
        this.taxaPendente = 0
    }
    sacar(valor) {
        if (valor > this.saldo + this.chequeEspecial) {
            return "Saldo insuficiente"
        }
        this.saldo -= valor
        // verificar se entrou no cheque especial
        if (this.saldo < 0) {
            this.taxaPendente = Math.abs(this.saldo) * 0.20
        }
        return `Saque de R$${valor} realizado`

}
*/
